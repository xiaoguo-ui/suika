# Suika 坐标系统转换详解

## 概述

Suika 编辑器使用了多层坐标系统来处理图形编辑中的复杂变换。本文档详细说明了各个坐标系统及其转换关系。

## 坐标系统层次

### 1. 浏览器坐标系 (Browser Coordinates)

- **获取方式**: `event.clientX`, `event.clientY`
- **原点**: 浏览器视口左上角
- **单位**: CSS 像素
- **用途**: 鼠标事件原始坐标

### 2. 画布坐标系 (Canvas Coordinates)

- **计算方式**: `clientX - offsetX`, `clientY - offsetY`
- **原点**: 画布 DOM 元素左上角
- **单位**: CSS 像素
- **用途**: 相对于画布的位置

### 3. 视口坐标系 (Viewport Coordinates)

- **原点**: 编辑器视口左上角 (0,0)
- **单位**: CSS 像素
- **特点**: 经过偏移校正，支持缩放显示
- **用途**: UI 交互和渲染坐标

### 4. 场景坐标系 (Scene Coordinates)

- **原点**: 场景左上角 (0,0)
- **单位**: 逻辑单位 (受缩放影响)
- **特点**: 图形元素的实际坐标，支持几何变换
- **用途**: 图形数据存储和几何计算

## 核心转换方法

### 浏览器坐标 → 视图坐标

```typescript
getCursorXY(event: { clientX: number; clientY: number }) {
  return {
    x: event.clientX - this.setting.get('offsetX'),
    y: event.clientY - this.setting.get('offsetY'),
  };
}
```

**参数**:

- `offsetX/offsetY`: 画布相对于浏览器的偏移量

### 视图坐标 → 场景坐标

```typescript
toScenePt(x: number, y: number, round = false) {
  const zoom = this.zoomManager.getZoom();
  const { x: scrollX, y: scrollY } = this.viewportManager.getViewport();

  let newX = scrollX + x / zoom;
  let newY = scrollY + y / zoom;

  if (round) {
    newX = Math.round(newX);
    newY = Math.round(newY);
  }

  return { x: newX, y: newY };
}
```

**数学公式**:

```
sceneX = scrollX + viewportX / zoom
sceneY = scrollY + viewportY / zoom
```

### 场景坐标 → 视图坐标

```typescript
toViewportPt(x: number, y: number) {
  const zoom = this.zoomManager.getZoom();
  const { x: scrollX, y: scrollY } = this.viewportManager.getViewport();

  return {
    x: (x - scrollX) * zoom,
    y: (y - scrollY) * zoom,
  };
}
```

**数学公式**:

```
viewportX = (sceneX - scrollX) * zoom
viewportY = (sceneY - scrollY) * zoom
```

### 一键转换：浏览器坐标 → 场景坐标

```typescript
getSceneCursorXY(event: { clientX: number; clientY: number }, round = false) {
  const { x, y } = this.getCursorXY(event);
  return this.toScenePt(x, y, round);
}
```

## 关键参数管理

### 视口管理 (ViewportManager)

```typescript
getViewport(): IRect {
  return {
    x: this.scrollX,        // 水平滚动偏移
    y: this.scrollY,        // 垂直滚动偏移
    width: parseFloat(this.editor.canvasElement.style.width),   // 画布显示宽度
    height: parseFloat(this.editor.canvasElement.style.height), // 画布显示高度
  };
}
```

### 缩放管理 (ZoomManager)

```typescript
getZoom(): number {
  return this.zoom;  // 当前缩放比例 (1.0 = 100%)
}
```

**缩放范围**: 0.1 - 10.0 (10% - 1000%)

### 画布偏移设置

```typescript
// 默认值
offsetX: 0,  // 画布水平偏移
offsetY: 0,  // 画布垂直偏移
```

## 实际应用场景

### 1. 图形绘制

```typescript
onStart(e: PointerEvent) {
  // 将鼠标位置转换为场景坐标用于创建图形
  this.startPoint = this.editor.getSceneCursorXY(e, true);
}

onDrag(e: PointerEvent) {
  // 计算图形尺寸
  const currentPoint = this.editor.getSceneCursorXY(e, true);
  const width = Math.abs(currentPoint.x - this.startPoint.x);
  const height = Math.abs(currentPoint.y - this.startPoint.y);
}
```

### 2. 图形选择

```typescript
// 检查鼠标是否悬停在图形上
const scenePoint = this.editor.getSceneCursorXY(e);
const topHitElement = getTopHitElement(this.editor, scenePoint);
```

### 3. 视图变换

```typescript
// 平移视图
setViewport({ x: newScrollX, y: newScrollY });

// 缩放视图
setZoom(2.0); // 200% 缩放
```

## 坐标转换矩阵

### 视图 → 场景变换矩阵

```
[sceneX]   [1/zoom    0       scrollX] [viewportX]
[sceneY] = [  0     1/zoom   scrollY] [viewportY]
[  1   ]   [  0       0         1  ] [    1     ]
```

### 场景 → 视图变换矩阵

```
[viewportX]   [zoom    0      -scrollX*zoom] [sceneX]
[viewportY] = [  0   zoom    -scrollY*zoom] [sceneY]
[    1    ]   [  0     0           1     ] [  1  ]
```

## 使用建议

### 1. 选择合适的坐标系

- **场景坐标**: 图形几何计算、数据存储
- **视图坐标**: UI 渲染、鼠标交互反馈
- **浏览器坐标**: 仅用于事件处理入口

### 2. 注意精度处理

- 图形定位通常需要 `round = true`
- 渲染和动画保持浮点精度

### 3. 性能优化

- 缓存常用坐标转换结果
- 避免在循环中重复转换

### 4. 边界情况处理

- 检查缩放范围限制
- 处理视口边界约束
- 考虑高 DPI 屏幕适配

## 总结

Suika 的坐标系统设计实现了：

- **精确的几何计算**: 场景坐标确保图形变换的准确性
- **灵活的视图控制**: 支持缩放、平移等视图变换
- **一致的用户体验**: 统一的坐标转换接口
- **高性能渲染**: 优化的坐标计算和缓存策略

这种多层坐标系统是现代图形编辑器架构的核心，为复杂的图形操作和视图变换提供了坚实的基础。
