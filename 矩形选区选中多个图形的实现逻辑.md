# 矩形选区选中多个图形的实现逻辑

本文档详细分析了 Suika 项目中矩形选区选中多个图形的完整实现流程。

## 目录

- [核心架构](#核心架构)
- [用户操作流程](#用户操作流程)
- [关键算法详解](#关键算法详解)
- [性能优化策略](#性能优化策略)
- [特殊情况处理](#特殊情况处理)

## 核心架构

### 策略模式设计

矩形选区功能采用策略模式实现，主要类结构：

```
SelectTool (主控制器)
├── SelectMoveTool        # 移动策略
├── DrawSelectionTool     # 矩形选区策略 ⭐
├── SelectRotationTool    # 旋转策略
└── SelectResizeTool      # 缩放策略
```

### 数据流转

```
用户操作 → SelectTool → DrawSelectionTool → getElementsInSelection → 几何相交检测 → SelectedElements
```

## 用户操作流程

### 1. 初始化阶段 (onStart)

#### 1.1 检查连选状态

```typescript
// 检查是否按下 Shift 键决定连选模式
if (this.editor.hostEventManager.isShiftPressing) {
  this.isShiftPressingWhenStart = true;
  // 保存当前选中状态，用于后续增量操作
  this.startSelectedGraphs = this.editor.selectedElements.getItems();
} else {
  // 清空原有选中，开始新的选择
  this.editor.selectedElements.clear();
}
```

#### 1.2 记录起始点

```typescript
// 将鼠标事件坐标转换为场景坐标
this.startPoint = this.editor.getSceneCursorXY(e);
```

#### 1.3 初始化选区显示

```typescript
// 在场景图中设置选区起点，开始显示选区框
this.editor.sceneGraph.setSelection(this.startPoint);
this.editor.render();
```

### 2. 拖拽阶段 (onDrag)

#### 2.1 实时位置更新

```typescript
// 持续跟踪鼠标在场景中的位置
this.lastMouseScenePoint = this.editor.getSceneCursorXY(e);
```

#### 2.2 选区矩形计算

```typescript
// 根据起始点和当前点计算选区矩形
const box = getRectByTwoPoint(this.startPoint, this.lastMouseScenePoint);

// 矩形计算逻辑：
// x = min(x1, x2), y = min(y1, y2)
// width = |x1 - x2|, height = |y1 - y2|
```

#### 2.3 核心：查找选区内元素

##### 普通选择模式

```typescript
// 获取选区内所有符合条件的图形
const graphsInSelection = getElementsInSelection(this.editor);

// 设置为当前选中元素
this.editor.selectedElements.setItems(graphsInSelection);
```

##### 连选模式 (Shift 键)

```typescript
// 获取原有选中元素的父级ID集合
const parentIdSet = getParentIdSet(this.startSelectedGraphs);

// 在选区内查找新元素（排除已在选中中的父级元素）
const graphsInSelection = getElementsInSelection(editor, parentIdSet);

// 恢复原有选中状态
this.editor.selectedElements.setItems(this.startSelectedGraphs);

// 对新元素进行切换操作（添加或移除）
this.editor.selectedElements.toggleItems(
  graphsInSelection.filter((item) => !parentIdSet.has(item.attrs.id)),
);
```

#### 2.4 实时渲染

```typescript
this.editor.render(); // 更新选区框和选中状态显示
```

### 3. 结束阶段 (afterEnd)

#### 3.1 清理选区显示

```typescript
// 清除选区框显示
this.editor.sceneGraph.selection = null;
this.editor.render();
```

#### 3.2 状态重置

```typescript
this.isShiftPressingWhenStart = false;
this.startSelectedGraphs = [];
// ... 其他状态清理
```

## 关键算法详解

### 元素查找算法 (getElementsInSelection)

#### 算法流程

1. 获取当前选区矩形
2. 转换为包围盒格式
3. 从画布根节点开始深度优先搜索
4. 对每个节点执行相交检测

#### 深度优先搜索实现

```typescript
const getElementsInSelectionDFS = (
  editor: SuikaEditor,
  box: IBox, // 选区包围盒
  node: SuikaGraphics, // 当前遍历节点
  parentIdSet: Set<string>, // 父级ID集合（连选模式）
): SuikaGraphics[] => {
  const graphicsArr: SuikaGraphics[] = [];

  for (const child of node.getChildren()) {
    // 跳过不可见或锁定的元素
    if (!child.isVisible() || child.isLock()) continue;

    if (parentIdSet.has(child.attrs.id)) {
      // 连选模式：递归处理子元素
      graphicsArr.push(
        ...getElementsInSelectionDFS(editor, box, child, parentIdSet),
      );
    } else if (child.intersectWithChildrenBox(box)) {
      // 相交检测通过，添加至结果集
      graphicsArr.push(child);
    }
  }

  return graphicsArr;
};
```

### 几何相交检测算法

#### 分层相交检测 (intersectWithChildrenBox)

```typescript
intersectWithChildrenBox(box: IBox) {
  // 非容器元素：直接检测自身
  if (!this.isContainer) {
    return this.intersectWithBox(box);
  }

  // 容器元素：先检测自身
  if (!this.intersectWithBox(box)) {
    return false; // 容器不相交，子元素肯定不相交
  }

  // 检测子元素
  const children = this.getChildren();
  for (const child of children) {
    if (child.isVisible() && child.intersectWithBox(box)) {
      return true; // 任一子元素相交即返回true
    }
  }

  return false;
}
```

#### 精确相交检测 (intersectWithBox)

##### 步骤 1：包围盒快速检测

```typescript
// 使用AABB（轴对齐包围盒）进行快速排除
if (!isBoxIntersect(box, this.getMinBbox())) {
  return false; // 包围盒不相交，肯定不相交
}
```

##### 步骤 2：旋转处理

```typescript
const rotate = this.getRotate();

if (!rotate || rotate % (Math.PI / 2) == 0) {
  // 无旋转或90度倍数旋转：直接返回相交
  return true;
} else {
  // 有旋转：使用SAT（分离轴定理）进行精确检测
  // 1. 将选区坐标转换到元素局部坐标系
  // 2. 计算旋转后的包围盒
  // 3. 检查是否存在分离轴
  // 4. 如果无分离轴，则相交
}
```

##### 包围盒相交判断 (isBoxIntersect)

```typescript
export const isBoxIntersect = (box1: IBox, box2: IBox) => {
  return (
    box1.minX <= box2.maxX && // box1右边 >= box2左边
    box1.maxX >= box2.minX && // box1左边 <= box2右边
    box1.minY <= box2.maxY && // box1下边 >= box2上边
    box1.maxY >= box2.minY // box1上边 <= box2下边
  );
};
```

## 性能优化策略

### 1. 分层检测优化

- **容器优先**：先检测容器自身，避免不必要的子元素检测
- **提前退出**：容器不相交时立即返回，不检测子元素

### 2. 包围盒优化

- **AABB 检测**：使用轴对齐包围盒进行快速粗略检测
- **减少计算**：避免对明显不相交的元素进行精确检测

### 3. 可见性过滤

```typescript
if (!child.isVisible() || child.isLock()) continue;
```

- 跳过不可见元素，避免无效计算
- 跳过锁定元素，符合编辑器设计逻辑

### 4. 增量更新

- 拖拽过程中实时更新，但避免频繁的全量重绘
- 使用选区包围盒缓存，减少重复计算

## 特殊情况处理

### 1. 连选模式 (Shift 键)

- **父级排除**：避免重复选择已选中元素的父级
- **增量操作**：支持添加/移除操作，保持原有选中状态
- **状态保存**：记录操作开始时的选中状态

### 2. 旋转元素处理

- **坐标变换**：将选区坐标转换到元素局部坐标系
- **SAT 算法**：使用分离轴定理处理任意角度旋转
- **精确检测**：确保旋转元素的正确选择

### 3. 分组元素处理

- **容器逻辑**：分组被视为容器，需要检测子元素
- **递归搜索**：深度优先遍历所有层级
- **性能平衡**：在精确性和性能间取舍

### 4. 空格键平移支持

```typescript
// 按下空格键时，支持在选区过程中平移选区
if (this.startPointWhenSpaceDown && this.lastDragPointWhenSpaceDown) {
  // 调整选区起始点，实现选区平移效果
}
```

## 总结

矩形选区功能的实现体现了以下设计原则：

1. **模块化**：采用策略模式，支持扩展
2. **性能优先**：多层优化，避免无效计算
3. **精确性**：支持旋转、缩放等复杂变换
4. **用户友好**：支持连选、平移等交互模式
5. **实时响应**：拖拽过程中持续更新选中状态

这种设计既保证了功能的完整性，又兼顾了编辑器的响应性能和用户体验。
