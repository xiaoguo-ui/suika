# Suika 碰撞检测优化策略

## 概述

Suika 项目并没有使用传统的四叉树 (QuadTree) 进行空间索引优化，而是采用了多层优化策略来实现高效的碰撞检测。

## 核心策略

### 1. 深度优先搜索 (DFS) + 逆序遍历

```typescript
override getHitGraphics(point: IPoint, options: IHitOptions) {
  const children = this.getChildren();
  // 从后往前遍历，优先检测顶层图形
  for (let i = children.length - 1; i >= 0; i--) {
    const child = children[i];
    const hitGraphics = child.getHitGraphics(point, options);
    if (hitGraphics) {
      return hitGraphics; // 找到第一个相交的图形即返回
    }
  }
  return null;
}
```

**优势**:

- **Z-order 正确性**: 后绘制的图形在视觉上层，优先检测
- **提前退出**: 找到第一个相交图形即停止搜索
- **简单高效**: 无需维护复杂的数据结构

### 2. 分层包围盒检测

#### 第一层: 容器自身检测

```typescript
intersectWithChildrenBox(box: IBox) {
  if (!this.isContainer) {
    return this.intersectWithBox(box); // 非容器直接检测
  }
  if (!this.intersectWithBox(box)) {
    return false; // 容器都不相交，子元素肯定不相交
  }
  // ... 继续检测子元素
}
```

#### 第二层: 子元素检测

```typescript
for (const child of children) {
  if (child.isVisible() && child.intersectWithBox(box)) {
    return true; // 任一子元素相交即返回true
  }
}
```

### 3. 精确几何检测

#### 包围盒快速检测

```typescript
intersectWithBox(box: IBox) {
  // 首先进行AABB包围盒检测
  if (!isBoxIntersect(box, this.getMinBbox())) {
    return false; // 快速排除
  }

  const rotate = this.getRotate();
  if (!rotate || rotate % (Math.PI/2) == 0) {
    return true; // 无旋转或90度倍数，直接返回相交
  } else {
    // 使用SAT算法进行精确检测
    // ... 处理旋转情况
  }
}
```

#### SAT (分离轴定理) 精确检测

```typescript
// 对于旋转图形，使用分离轴定理
const [s1, s2, s3, s4] = rectToVertices(
  boxToRect(box),
  invertMatrix(this.getWorldTransform()),
);

const minX = Math.min(s1.x, s2.x, s3.x, s4.x);
const minY = Math.min(s1.y, s2.y, s3.y, s4.y);
const maxX = Math.max(s1.x, s2.x, s3.x, s4.x);
const maxY = Math.max(s1.y, s2.y, s3.y, s4.y);
```

### 4. 缓存优化策略

#### 包围盒缓存

```typescript
protected _cacheBboxWithStroke: Readonly<IBox> | null = null;
protected _cacheBbox: Readonly<IBox> | null = null;
protected _cacheMinBbox: IBox | null = null;

// 缓存失效时清除
invalidateBboxCache() {
  this._cacheBbox = null;
  this._cacheBboxWithStroke = null;
  this._cacheMinBbox = null;
}
```

#### 惰性计算

```typescript
getBboxWithStroke(): IBox {
  if (this._cacheBboxWithStroke) {
    return this._cacheBboxWithStroke; // 直接返回缓存
  }
  // 计算包围盒
  const bbox = this.computeBboxWithStroke();
  this._cacheBboxWithStroke = bbox;
  return bbox;
}
```

## 性能优化点

### 1. 过滤策略

#### 可见性过滤

```typescript
if (!child.isVisible() || child.isLock()) continue;
```

#### 父级过滤 (连选模式)

```typescript
if (parentIdSet.has(child.attrs.id)) {
  // 递归处理子元素，但不直接选中父级
  graphicsArr.push(
    ...getElementsInSelectionDFS(editor, box, child, parentIdSet),
  );
}
```

### 2. 检测顺序优化

1. **粗略检测**: 包围盒快速检测
2. **精确检测**: 仅对通过粗略检测的图形进行精确几何检测
3. **变换处理**: 优先处理无旋转的简单情况

### 3. 内存优化

- **缓存重用**: 包围盒计算结果缓存
- **按需计算**: 只有在需要时才计算复杂几何
- **引用透明**: 避免重复创建对象

## 与四叉树的对比

| 特性           | Suika 当前策略        | 四叉树方案            |
| -------------- | --------------------- | --------------------- |
| **实现复杂度** | 低                    | 高                    |
| **内存占用**   | 低                    | 中等                  |
| **维护成本**   | 低                    | 高                    |
| **动态更新**   | 简单                  | 复杂                  |
| **查询性能**   | 线性 (O(n))           | 对数 (O(log n))       |
| **适用场景**   | 小型文档 (<1000 元素) | 大型文档 (>1000 元素) |

## 为什么不使用四叉树？

### 1. 应用场景特点

- **文档规模**: 大多数用户文档元素较少 (<500 个)
- **动态性**: 图形经常移动、缩放、删除
- **交互频率**: 鼠标悬停检测非常频繁

### 2. 维护成本考虑

- **重建开销**: 每次图形变化都需要重建索引
- **内存开销**: 需要额外的索引数据结构
- **复杂度增加**: 调试和维护更加困难

### 3. 性能权衡

```typescript
// 对于 n=1000 的情况:
// DFS遍历: O(1000) = 1000次操作
// 四叉树查询: O(log 1000) ≈ 10次操作
// 但重建索引的开销可能超过查询优化带来的收益
```

## 优化建议

### 当前策略的优势

- **简单可靠**: 实现和调试都比较容易
- **内存友好**: 不需要额外的数据结构
- **动态友好**: 支持频繁的图形变化

### 可能的改进方向

1. **混合策略**: 对大型文档使用四叉树，小文档使用当前策略
2. **局部索引**: 只对频繁交互的区域建立索引
3. **预测缓存**: 根据鼠标移动趋势预计算可能的碰撞

### 性能监控点

- 鼠标悬停时的检测耗时
- 大文档下的选择操作性能
- 内存使用情况监控

## 总结

Suika 的碰撞检测策略体现了**实用主义**的设计哲学：

1. **保持简单**: 使用直观的 DFS 遍历而不是复杂的空间索引
2. **分层优化**: 粗略检测 + 精确检测 + 缓存优化
3. **场景适配**: 根据实际使用场景选择合适的优化策略
4. **性能平衡**: 在实现复杂度、内存占用和查询性能间取舍

这种设计既保证了功能的正确性，又兼顾了实现的简洁性和维护性。
