---
description: 当用户要求为某个功能生成 spec 文档时，遵循此规则
globs:
  - specs/*.spec.md
---

# 功能 Spec 文档生成规则

## 触发条件
当用户说"帮我生成 xxx 的 spec"、"帮我分析 xxx 功能"、"帮我写 xxx 的文档"时，按此规则生成。

## 输出位置
所有 spec 文档统一放在 `specs/` 目录下，命名格式：`功能名.spec.md`（英文短横线连接）。

## 文档结构（严格按此顺序）

### 一、功能概述
一句话说清这个功能是什么。不写"功能价值"，不写废话。

### 二、用户操作流程
用编号列表写出用户的操作步骤，格式：`用户做 X → 系统响应 Y`。
包含主流程和主要分支（如"若未拖拽则..."）。不画流程图。

### 三、业务规则
每条规则包含以下字段：
- **代码线索**：用于搜索的关键词（函数名、变量名）
- **预期位置**：大概在哪个环节出现
- **验证方法**：怎么操作编辑器能验证这条规则
- **验证结果**：✅ 已找到 / ❌ 未找到（必须实际搜索代码验证）
- **代码位置**：文件名:行号（必须是真实行号，从代码搜索结果中获取）
- **为什么这么设计**：回答"如果不这么做会怎样"。这是最重要的一项，必须写清楚设计动机、替代方案的缺点、以及这个选择的优势。

**关键要求**：
- 每条规则的"验证结果"和"代码位置"必须通过实际搜索代码来填写，不能猜测
- "为什么这么设计"要回答：不这么做会怎样？有没有其他方案？为什么选了这个？
- 规则同时覆盖正常行为和边界情况，不单独分节

### 四、耦合点
用表格列出与其他系统的关系：

| 耦合系统 | 触发时机 | 代码线索 | 代码位置 | 为什么需要这个耦合 |

**关键要求**：
- "为什么需要这个耦合"必须说清楚：如果去掉这个耦合会怎样、为什么不能内联处理
- 代码位置必须是真实行号

### 五、核心代码路径
用表格列出从入口到出口的完整调用链：

| 步骤 | 文件 | 行号 | 说明 |

### 六、架构设计决策（知其所以然）
列出 3-5 个关键的"为什么"问题并回答。格式：

```
### 为什么 XXX？
因为...如果不这么做...替代方案的问题是...
```

这一节是面试时最有价值的部分，要写出：
- 设计动机
- 替代方案及其缺点
- 涉及的设计模式（如有）

### 七、一句话说清
用一句话总结整个功能的实现方式，包含关键技术决策。

## 工作流程

1. **先探索代码**：用搜索工具找到功能涉及的所有文件和关键函数
2. **逐条验证规则**：每条规则都要实际搜索代码，填上真实行号
3. **验证耦合点**：搜索每个耦合系统的调用代码，确认真实位置
4. **提炼"为什么"**：对每个设计决策，思考"不这么做会怎样"
5. **生成文档**：按模板结构输出到 `specs/` 目录

## 质量标准

- 所有行号必须来自实际代码搜索，不能编造
- 每条"为什么"必须包含"替代方案"和"替代方案的缺点"
- 文档应该让读者能"说出来"这个功能是怎么实现的
- 目标读者是想理解代码但不需要改代码的人
